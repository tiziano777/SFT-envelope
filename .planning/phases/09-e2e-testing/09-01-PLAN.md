# Phase 9.1: E2E Lifecycle & Simulation Tests

**Goal:** Create reusable Worker/Master simulators and test the complete experiment lifecycle from handshake through training completion.

**Duration:** ~2 hours | **Deliverables:** 5 test files + fixtures | **Status:** DEFERRED

**Deferred Due To:** Phase 11 async migrations took priority. E2E test suite design remains valid; implementation deferred pending completion of Phase 11 manual testing verification and core feature stabilization.

---

## Requirements Mapping

- TEST-01: conftest.py with Neo4j Docker fixture and FastAPI TestClient
- TEST-02: simulate_worker.py — simulates complete E2E training
- TEST-03: simulate_master.py — starts master in subprocess
- TEST-05: test_checkpoint_sync.py — push ckp, idempotency, uri=NULL
- TEST-07: test_daemon_lifecycle.py — startup/stop/crash recovery
- TEST-08: Test nodes with _TEST label, cleanup DETACH DELETE

---

## Implementation Tasks

### Task 1: Create `tests/conftest.py` — Test Fixtures & Environment

**Goal:** Setup Neo4j Docker fixture, FastAPI test client, and test database cleanup

**Files:** `tests/conftest.py` (new)

**Acceptance Criteria:**
- ✓ neo4j_container fixture starts Docker Neo4j on ephemeral port
- ✓ neo4j_session initializes schema and cleans _TEST nodes
- ✓ app_client returns FastAPI TestClient
- ✓ worker_simulator provides reusable WorkerSimulator
- ✓ master_process subprocess with health check
- ✓ Proper cleanup (close, stop, terminate)

---

### Task 2: Create `tests/utils/simulate_worker.py` — Worker Simulator

**Goal:** Reusable Worker simulator for E2E tests

**Files:** `tests/utils/simulate_worker.py` (new)

**Methods:**
- `handshake(config_dict, strategy_override=None)` → HandshakeResponse
- `checkpoint_push(ckp_num, artifact_uri=None, event_id=None)` → dict
- `sync_event(event_type, payload)` → dict
- `training_done()` → dict
- `close()` → cleanup

**Acceptance Criteria:**
- ✓ All methods send X-API-Key header
- ✓ All methods add _TEST tag for cleanup
- ✓ checkpoint_push handles artifact_uri=None
- ✓ Returns correct response types
- ✓ Reusable across tests

---

### Task 3: Create `tests/utils/simulate_master.py` — Master Launcher

**Goal:** Start Master API subprocess with auto-port and health check

**Files:** `tests/utils/simulate_master.py` (new)

**Functions:**
- `find_free_port()` → int
- `start_master(port=None, timeout=10)` → (Popen, port)
- `stop_master(proc, timeout=5)` → exit_code

**Acceptance Criteria:**
- ✓ find_free_port returns available port
- ✓ start_master spawns subprocess with env vars
- ✓ Health check waits for /health 200
- ✓ stop_master terminates gracefully
- ✓ Works on macOS and Linux

---

### Task 4: Create `tests/test_daemon_lifecycle.py` — Daemon Tests

**Goal:** Test daemon startup, shutdown, state persistence

**Files:** `tests/test_daemon_lifecycle.py` (new)

**Tests:**
- `test_daemon_bootstrap` — .exp_id and .handshake_done files
- `test_daemon_signal_handling` — SIGTERM graceful shutdown
- `test_state_persistence` — state survives restart
- `test_transfer_log_append` — atomicity and append semantics

**Acceptance Criteria:**
- ✓ All 4 tests pass

---

### Task 5: Create `tests/test_checkpoint_sync.py` — Checkpoint Tests

**Goal:** Test checkpoint push idempotency and edge cases

**Files:** `tests/test_checkpoint_sync.py` (new)

**Tests:**
- `test_push_checkpoint_idempotency` — same event_id → 200
- `test_push_checkpoint_no_uri` — uri=NULL handling
- `test_push_checkpoint_artifact` — artifact_uri verification
- `test_state_persistence_after_push` — atomic write + crash recovery

**Acceptance Criteria:**
- ✓ All 4 tests pass
- ✓ Test nodes tagged :_TEST and cleaned

---

## Success Definition

**Plan 9.1 done when:**
1. conftest.py fixtures work: neo4j_container, neo4j_session, app_client, worker_simulator, master_process
2. simulate_worker.py and simulate_master.py importable and functional
3. test_daemon_lifecycle.py: 4/4 passing
4. test_checkpoint_sync.py: 4/4 passing
5. 0 test nodes in Neo4j after suite
6. Total: 8+ tests passing in < 30s
